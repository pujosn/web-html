apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-app-9090
spec:
  # revisionHistoryLimit: 5
  replicas: 3
  selector:
    matchLabels:
      app: simple-app
  template:
    metadata:
      labels:
        app: simple-app
      # namespace: backend
    spec:
      containers:
      - name: simple-app
        image: pujosn/web-app:1.1.2
        imagePullPolicy: Always
        resources:
          limits:
            memory: "20Mi"
            cpu: "20m"
        ports:
        - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: simple-app-9090-service
spec:
  selector:
    app: simple-app
  ports:
  - protocol: TCP
    port: 9090
    targetPort: 9090
  type: LoadBalancer
